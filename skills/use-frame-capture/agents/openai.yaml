interface:
  display_name: "Use frame-capture"
  short_description: "Capture routes for host-owned Rust renderers"
  default_prompt: "Use $use-frame-capture to add deterministic screenshot routes to a Rust app with a host-owned renderer."
