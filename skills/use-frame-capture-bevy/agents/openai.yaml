interface:
  display_name: "Use frame-capture Bevy"
  short_description: "Bevy live and offscreen capture wiring"
  default_prompt: "Use $use-frame-capture-bevy to add deterministic offscreen screenshot capture to a Bevy app."
